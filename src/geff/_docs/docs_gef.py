DOCS = {
    "module":"""
    This module defines the main `BaseGEF` class and its class factory `make_model`.
    """,
    "BaseGEF":"""
    This class is the primary interface of a GEF model.

    The class defines how its `GEFSolver` and `ModeSolver` are used to solve GEF equations in `run`.

    The `BaseGEF` is configured to the model `.models.pai`. To define a custom GEF model, use the class factory `make_model`.
    """
}
